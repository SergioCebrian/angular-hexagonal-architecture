import { DeleteTaskUseCase } from './delete-task-usecase';

describe('DeleteTaskUseCase', () => {
  it('should create an instance', () => {
    expect(new DeleteTaskUseCase()).toBeTruthy();
  });
});
